PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX haOrg: <https://data.hetarchief.be/ns/organization/>

SELECT DISTINCT 
?organization_id
?ha_pref
?created_at
?updated_at
WHERE {
  ?id a haOrg:ContentPartner;
      haOrg:hasPreference/skos:prefLabel ?ha_pref .
}
